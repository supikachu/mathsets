-- =============================================================================
-- 打标专用模型槽位
--
-- 打标的两阶段（抽关键词 / 从候选菜单选 ID）是结构化分类任务，用推理模型
-- （如 stealth/ox-alpha，实测均值 90s/次）纯属浪费。拆出独立槽位后，解析可以
-- 继续用强模型，打标走快模型。为空时回退到 model_text，行为与改动前一致。
-- =============================================================================

ALTER TABLE user_ai_settings
    ADD COLUMN IF NOT EXISTS model_tagging TEXT;
