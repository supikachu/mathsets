# 复制为同目录下的 credentials.txt 后填写（credentials.txt 已被 git 忽略）
# 也可直接编辑 bench/credentials.txt（脚本已生成空模板）
#
# 账号必须是本机已注册、且仍有「解析任务」日额度的用户。
# 后端需已启动。默认连 http://127.0.0.1:3000

username=
password=
base_url=http://127.0.0.1:3000
