# 2026-08-01 工作日志

## 知识点树面板重构 — Phase 1（P0 Bug 修复）已完成

背景：重构 QuestionEdit.vue 右侧 AttributeSidePanel 的知识树标注区。完整计划在 `~/.workbuddy/cosmic-cascade-turing.md`。

**架构底线（用户明确要求，后续 Phase 必须遵守）：**
- 后端零改动，知识节点只走扁平 `knowledge_node_ids` → `question_knowledge_nodes` 关联表，禁止写入 metadata JSONB
- 分类只靠前端 `tree_id -> kind` 映射（knowledgeTreeApi.list()）
- 不用虚拟滚动，性能靠扁平索引 Map
- 学段切换无弹窗，用内存缓存 Map

**已确认的交互语义：**
- 级联选择：勾选/取消父节点都级联到所有子孙
- AI 高亮消除：手动触碰单个即消 + 保存成功全清
- 切换缓存范围：仅三组树节点 ID

**Phase 1 改动（已 vue-tsc 验证，仅剩 QuestionList.vue 一个存量报错与本次无关）：**
1. `AttributeSidePanel.vue` `.asp-tree-inline` 加 `max-height: min(420px,52vh)` + 内部滚动，修复 1500px 裁切（Tabs/收起按钮在滚动区外常驻）
2. `QuestionEdit.vue` `doRestoreDraft` 补齐 chapterNodeIds / methodNodeIds 还原
3. `QuestionEdit.vue` 分类兜底重构：抽出 `distributeNodesByTreeKind()`；list() 失败时暂存 `pendingNodes`（含 tree_id）不静默错分；顶部琥珀色提示条「重试分类」`retryDistributePendingNodes()`；`buildPayload` 把 pendingNodes 原样并入 mergedNodeIds

## 知识点树面板重构 — Phase 2（KnowledgeTreeCheckbox 核心重构）已完成

`components/KnowledgeTreeCheckbox.vue` 整体重写，对外契约不变（`nodes` + `v-model`），新增可选 prop `highlightIds`：
- 根实例 DFS 构建 `nodeIndex: Map<nodeId,{parentId,childrenIds,name,namePath}>`，`provide(TREE_CTX_KEY)` 共享给递归子层（inject 到 null 即为根）
- 对称级联 toggle：勾选/取消都作用于自身+全部子孙（collectDescendants 迭代 O(子树)）
- 三态：`indeterminateSet` computed 沿 parentId 向上标记祖先，O(k·深度)
- 吸顶工具条：搜索（按 name 匹配，可见集=命中+祖先链，过滤期间强制展开）+ 展开/收起全部 + 仅看已选；`<mark>` 关键词高亮
- 默认收起（nodes 变化时 expandedIds 重置为空 + 清空搜索）
- 点击行为：行点击=级联勾选，箭头=折叠（过滤期间折叠禁用），checkbox 与行同效
- AI 高亮 `.is-ai-highlight`：rgba(250,204,21,.14) 微光 + 左侧金色描边；移除 question_count 药丸；行 title=namePath；连接线/hover 全部主题变量化

验证：vue-tsc 通过；vite build 编译通过（默认 dist 被 safe-delete shim 拦截属环境问题，用 --outDir dist-verify 验证后已清理）。
注意：本机 rm -rf 会被 WorkBuddy safe-delete 层拦截且路径解析有 bug（/c/... 转 \c\...），清理产物用 PowerShell Remove-Item。

## 知识点树面板重构 — Phase 3（状态集成）已完成，全部四阶段收官

新增 `frontend/src/composables/useKnowledgeTreeCache.ts`（共享缓存）：
- `getKnowledgeTreeList()`：树列表全量只拉一次（含并发去重），AttributeSidePanel 与 QuestionEdit 共用
- `getKnowledgeTreeData(treeId)`：单树按 treeId 缓存，Tab/学段切换零请求
- `buildTreeMetaIndex()`：扁平 meta（parentId/namePath）供 chips 折叠

`AttributeSidePanel.vue`：
- `runAiTagging` 知识点回填改并集（`new Set([...old, ...ai])`），新增 ID 进 `aiHighlightIds`（defineModel 提升至 QuestionEdit）
- 高亮消除双条件：setter 内对新旧数组取对称差集移除触碰节点（含级联波及项）；QuestionEdit handleSave 成功后 `aiHighlightIds.value = []`
- 学段/学科切换 watch：旧 key `${subject}_${stage}` 快照三组 ID 入 props.selectionCache（QuestionEdit 持有的 plain Map），新 key 命中瞬恢、未命中清空；切换时清 AI 高亮
- Tab 徽标（modeCounts，0 隐藏）+ method Tab 无树时 disabled + tooltip（methodTreeAvailable 按 treeList code 匹配）
- chips 折叠 collapseToTopmost：沿 parentId 找最高层已选节点，下级代管数 `+N`，title=namePath
- 树组件接 `:highlight-ids="aiHighlightIds"`

`QuestionEdit.vue`：aiHighlightIds ref、selectionCache Map（plain，非响应式）、v-model/props 传给面板、distributeNodesByTreeKind 改走 getKnowledgeTreeList、handleSave 清高亮

## UI 瘦身精修（用户反馈后执行，为最终形态）

用户反馈工具条在递归树中视觉上重复、层级不明、嵌套滚动灾难，要求极简纯粹树：
- `KnowledgeTreeCheckbox.vue`：彻底删除搜索框/展开全部/收起全部/仅看已选及全部相关状态（visibleSet、nameSegments 等），回归纯净多选树；保留扁平 Map、对称级联、三态预计算、AI 金色高亮、title=namePath
- 层级：每级 margin-left 7px + padding-left 17px（24px 级距）+ `border-left: 1px solid var(--border-color)` 竖直引导线贯穿整组（替代原虚线肘形连接线）
- 复选框 16×16px（对勾/半选条位置同步调整），行 `gap: 8px` + min-height 32px 绝对居中
- `AttributeSidePanel.vue`：`.asp-tree-inline` 移除内部滚动（max-height/overflow 全删），树自然撑长，滚动归最外层面板；`.asp-tree-collapse` 从 max-height:1500px 改为 `grid-template-rows: 0fr/1fr` 动画（内层 .asp-tree-collapse-inner overflow:hidden），展开态卡片样式挂内层——彻底根除 1500px 裁切

## UI 二次精修（组卷网文件树标准，为最终形态）

- 复选框回收到 14×14px 标准尺寸（对勾 left4/top1 w4 h8；半选条 left3/top6 w6 h2），无任何 transform 缩放
- 节点名称废弃单行截断：`white-space: normal; word-break: break-all; line-height: 1.5` 自然换行
- 行 Flex 改 `align-items: flex-start`；箭头 margin-top 2px、复选框 margin-top 3px、占位同 2px——锚定首行文字中心（5px padding + 13px×1.5/2 ≈ 15px）
- 层级线恢复肘形虚线引导：子 ul padding-left 18px；li::before 竖虚线 #dcdfe6 贯穿、li::after 横虚线 top 15px、last-child 竖线截断收口；暗色主题走 var(--border-color)

经验：此组件的视觉基准 = 组卷网文件树（肘形虚线 + 多行文本 + 顶部对齐），用户在两轮反馈中最终拍板，勿再改成通栏竖线或单行截断。
